
export interface AuthButtonPropsType {
    id: string,
    buttonText: string,
    width: string,
    textColor: string,
    backgroundColor: string,
    disable: boolean,
    handleClick: ()=> void;
}