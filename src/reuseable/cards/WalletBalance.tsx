import React from 'react';

const WalletBalance = () => {
    return (
        <div
            className={` w-full h-[12rem] rounded-md bg-gradient-to-r from-[#142854] to-[#093086] `}
        >

        </div>
    );
};

export default WalletBalance;